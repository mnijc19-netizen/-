# 极智全域个人资产与财务管理系统 (SmartWealth Pro) — 完整设计架构与技术规范全书

> **文档版本**: v2.4 (Complete History & Specification Edition)  
> **最后更新**: 2026-08-25  
> **核心用途**: 保证任何新接力的 AI 模型 / 开发者 100% 了解用户的全部想法、历史提出的每一个需求和修改细节，无缝接力继续开发。

---

## 目录
1. [用户历史提出的全部需求与修改指令清单 (Complete User Requirements History)](#1-用户历史提出的全部需求与修改指令清单)
2. [防放弃与零摩擦记账体系 (Zero-Friction Bookkeeping)](#2-防放弃与零摩擦记账体系)
3. [iPhone 硬件与自动化集成技术规范](#3-iphone-硬件与自动化集成技术规范)
4. [双引擎运行架构 (Dual-Engine Architecture)](#4-双引擎运行架构)
5. [智能识别与解析算法矩阵](#5-智能识别与解析算法矩阵)
6. [数据模型与本地数据库设计](#6-数据模型与本地数据库设计)
7. [版本演进与全量修改日志 (Changelog)](#7-版本演进与全量修改日志)
8. [给后续接力 AI 模型的开发指引](#8-给后续接力-ai-模型的开发指引)

---

## 1. 用户历史提出的全部需求与修改指令清单

以下是用户在整个项目生命周期中提出的**全部核心需求、痛点与修改指令**（按时间线完整记录）：

### 📝 需求 1：全域财富聚合与自动记账
* **用户原文**：*“帮我做一个个人的财务管理系统，必须面面俱到，并且能记账或者可以的话直接识别手机信息来自动记账？而且财富可能分散在各种地方，给我完善设计好。”*
* **实现方案**：
  - 设计覆盖 10 大资产类型的全域矩阵（现金、微信钱包、支付宝、银行储蓄卡、证券A股/美股、加密货币、房产车辆固定资产、对外借款应收、信用卡负债、房贷按揭）；
  - 研发 20+ 国内主流银行短信与微信/支付宝通知文本正则及 NLP 智能解析引擎。

### 📝 需求 2：杜绝“记录繁琐导致中途放弃”
* **用户原文**：*“看着可以，但我担忧的是记录麻烦，我怕因为每天消耗不少时间在这上面，我有一天开始不记录了，你想个办法。”*
* **实现方案**：
  - 打造五重自动化减负体系：剪贴板智能提取、微信/支付宝官方 CSV 账单月度 10 秒批量导入去重、周期性固定收支自动入账、懒人余额快照模式（大数法则只填各大账户总余额自动推算净资产变动）。

### 📝 需求 3：全面转向手机移动端、清空假数据、引入图片识别
* **用户原文**：*“这个网页完全是为手机制作的，请你改成手机适配的网页，并且删除里面记录过的余额或往期记录，并且引入识别图片等功能。”*
* **实现方案**：
  - 将 UI 全面重构为 Mobile-First 手机原生体验（底部 iOS 悬浮导航栏、刘海屏与手势条 Safe Area 适配、大拇指触控卡片）；
  - 清空预设的百万假资产与历史流水，将默认账户余额全部归零，提供纯净的个人新账本；
  - 引入浏览器本地 Tesseract.js 图片 OCR 与小票/截图识别功能。

### 📝 需求 4：iPhone 硬件级一键自动化（长按侧边按键秒存）
* **用户原文**：*“图片识别完全不准确，而且记账依然有点麻烦，有没有更好的方法，或者我每次付钱后，长按 iphone 的按钮就自动记录进去。”*
* **实现方案**：
  - 接入 iPhone 15 Pro / 16 操作按钮 (Action Button) 与轻点背面 (Back Tap) 快捷指令；
  - 设计 URL 自动化协议 `https://mnijc19-netizen.github.io/-/?text=[URL编码文本]` 与 `?cb=1`；
  - 网页接收到参数后，**0 步点击直接自动写入账本，并弹出成功横幅与彩带动效**。

### 📝 需求 5：微信聊天截图逆向精准提取与“请选择账户”修复
* **用户原文**：*“图片不准而且识别奇怪，‘手机截屏消费’实在突兀，并且我设置快捷指令，能不能他打开后直接记录，这样设置也只是打开了而已……还有，我添加资金的时候，微信可能被设置为类似‘请选择账户’了，我点记录它显示‘请选择账户’。”*
* **实现方案**：
  - **重构图片逆向识别算法**：自动过滤上方“微信记账日报昨日支出”等干扰文字，精准逆向锁定最底部的最新支付卡片（如 `麦当劳 ¥14.90`），彻底剔除生硬商户名；
  - **修复账户下拉框异步未选中问题**：所有弹窗（快捷记账、智能识别等）在打开时自动智能锁定首选有效账户（如「微信零钱」），彻底解决“请选择账户”报错；
  - **解决 Safari URL 编码多行文本截断问题**：快捷指令通过「URL 编码」动作将多行文字合法化，配合前端全量 URL 正则解析，实现打开即 100% 自动入账。

---

## 2. 防放弃与零摩擦记账体系

```
                                  ┌── 1. iPhone 硬件动作按钮/双击背面 (0秒截屏/文字自动落库)
                                  ├── 2. 手机短信/通知复制即记账 (<2秒，自动提取金额/商户/分类)
【SmartWealth 零摩擦体系】 ───────┼── 3. 微信/支付宝官方CSV账单批量导入 (平时不记，月底10秒全量同步)
                                  ├── 4. 懒人余额快照模式 (大数法则，填总余额自动推算净资产)
                                  └── 5. 周期性固定流水自动生成 (工资、房贷、订阅扣款每月自动入账)
```

---

## 3. iPhone 硬件与自动化集成技术规范

### 3.1 快捷指令与 URL 协议规范
系统通过 URL 路由参数与 iOS 快捷指令（Shortcuts）无缝联动：

| URL 格式 | 触发行为 | 执行逻辑 |
| :--- | :--- | :--- |
| `https://mnijc19-netizen.github.io/-/?text=[URL编码文本]` | 截屏/短信/语音自动记账 | 逆向提取金额与商户，0点击直接写入账本，弹出绿色提示与礼花 |
| `https://mnijc19-netizen.github.io/-/?cb=1` | 剪贴板自动秒存 | 自动读取 `navigator.clipboard`，提取要素并落库 |
| `https://mnijc19-netizen.github.io/-/?amt=58&mer=麦当劳` | 结构化快速记账 | 直接以指定金额与商户入账 |

### 3.2 iOS 快捷指令配置最佳实践（必须经过 URL 编码）
* **动作序列**：
  1. 动作 1：`获取屏幕内容`（或 `获取最新的屏幕快照`）
  2. 动作 2：`从 获取屏幕内容 中提取文本`
  3. 动作 3：**`URL 编码`**（关键：将多行文本转换为合法 URL 编码，避免 Safari 截断）
  4. 动作 4：`打开 URL` → `https://mnijc19-netizen.github.io/-/?text=[URL 编码的文本]`

---

## 4. 双引擎运行架构 (Dual-Engine Architecture)

系统实现了**一套前端代码、双模式自适应运行**：

### 模式 A：纯静态离线模式 (GitHub Pages / 手机独立 PWA)
* **宿主**：`https://mnijc19-netizen.github.io/-/`
* **存储引擎**：`frontend/src/services/localStore.ts` (HTML5 LocalStorage / IndexedDB)
* **特点**：0 毫秒响应、离线可用、无服务器依赖、数据完全保留在手机本地。

### 模式 B：本地私有化服务模式 (Local FastAPI + SQLite)
* **宿主**：`http://127.0.0.1:8000` / `http://局域网IP:8000`
* **后端**：Python 3.10+ FastAPI (`backend/main.py`)
* **数据库**：SQLite (`backend/finance.db`)，包含 10 张完整业务数据表。

---

## 5. 智能识别与解析算法矩阵

### 5.1 微信/支付宝聊天截屏逆向定位算法 (`imageOcr.ts` & `urlAutoIngest.ts`)
* **算法策略**：从文本底部向上（Bottom-Up）逆向扫描：
  - 锁定最下方的支付卡片金额（如 `¥ 14.90`）；
  - 提取金额上方 1~2 行的商户名称（如 `麦当劳`、`瑞幸咖啡`）；
  - 提取扣款方式（如 `使用零钱支付`、`招商银行卡`）并自动匹配扣款账户。

### 5.2 国内 20+ 主流银行与短信 NLP 提取引擎 (`sms_parser.ts` & `sms_parser.py`)
* 支持招行、工行、建行、农行、中行、交行、浦发、中信、广发、平安、民生、兴业、光大等。
* 支持微信支付凭证、支付宝交易通知、云闪付及自然语言一句话（如“昨晚海底捞吃了320招行卡”）。

---

## 6. 数据模型与本地数据库设计

```
accounts (账户矩阵: 现金/银行卡/微信/支付宝/证券/房产/负债)
   │
   ├── transactions (流水账目: 收入/支出/转账/还款，关联账户余额双向联动)
   ├── budgets (月度总预算与各分类子预算)
   ├── investments (股票/基金/ETF/加密货币，跟踪持仓市值与浮动盈亏)
   ├── debts (信用卡免息期、房贷月供、雪球/雪崩还款模拟)
   ├── goals (心愿目标、储蓄进度、彩带庆贺)
   ├── asset_snapshots (懒人余额快照与净资产推算)
   └── recurring_rules (周期性固定收支自动生成)
```

---

## 7. 版本演进与全量修改日志 (Changelog)

| 版本 | 关键改动与优化项 |
| :--- | :--- |
| **v1.0** | 完成后端 FastAPI + SQLite 10张数据表、短信正则引擎、微信支付宝 CSV 批量导入、财务大盘分析服务。 |
| **v1.5** | 完成 React 19 + TypeScript + Tailwind v4 + ECharts 10 大业务功能页面搭建，通过单元测试。 |
| **v2.0** | 接入 GitHub 仓库与 GitHub Actions 自动化部署工作流，开发纯静态离线 LocalStorage 引擎。 |
| **v2.1** | 全面重构为 Mobile-First 手机原生体验（底部 iOS 悬浮导航栏、刘海屏 Safe Area 适配、清空初始假数据）。 |
| **v2.2** | 引入图片 OCR 识别，重构微信/支付宝账单气泡逆向精准定位算法，剔除生硬商户名。 |
| **v2.3** | 打造 iPhone 操作按钮 (Action Button) 与快捷指令 0 步自动记账，修复 Safari 多行 URL 编码截断与账户自选问题。 |
| **v2.4** | 编写详尽的《项目设计架构与技术规范全书》，将用户的全部诉求与修改记录写入规范，保证跨模型接力零信息丢失。 |
| **v2.5** | 完美打通 iPhone 硬件长按秒级自动记账：自适应兼容微信支付卡片与支付宝账单详情，清洗麦当劳 M 等图标杂讯，自动绑定支付宝/花呗账户。 |

---

## 8. 给后续接力 AI 模型的开发指引

1. **阅读顺序**：接力本项目的 AI 模型，请优先通读本文档第 1 节（用户全部历史需求）与第 4 节（双引擎架构）。
2. **修改前端页面时**：优先保留 `frontend/src/services/localStore.ts` 与 `frontend/src/api/client.ts` 中的 `isStaticMode` 静态离线兼容逻辑。
3. **修改快捷指令逻辑时**：确保 `urlAutoIngest.ts` 能正确处理 `URL 编码` 后的 multi-line 截屏文字与参数。
4. **构建与部署**：在 `frontend/` 目录下运行 `npm run build`，产物会自动由 GitHub Actions 发布到 GitHub Pages。
