# 👑 AGENTS.md — AI 代理开发全域指南与宪法总纲

> **致接力 AI 代理（Antigravity / Cursor / Claude Code / Windsurf / GPT）：**
> 在开始阅读任何代码或执行任何修改前，你必须**无条件阅读并完全遵守**本文件及 `docs/MASTER_CONSTITUTION.md` 中的全部母宪法公理与工程治理准则。

---

## 🏛️ 一、项目全景与架构层级 (5-Layer Onion Architecture)

本项目为 **极智全域个人与家庭资产智能财务管理系统 (SmartWealth Pro · 斌斌钱包)**。
系统严格划分为 5 层单向洋葱依赖架构，严禁越级或逆向调用：

```
┌─────────────────────────────────────────────────────────────┐
│ Layer 4: UI 展现与交互层 (components/, pages/, Apple HIG 玻璃态) │
├─────────────────────────────────────────────────────────────┤
│ Layer 3: 响应式状态与持久化层 (localStore.ts, api/client.ts)   │
├─────────────────────────────────────────────────────────────┤
│ Layer 2: 领域纯计算引擎层 (repaymentScheduler, marketData)    │
├─────────────────────────────────────────────────────────────┤
│ Layer 1: 纯数据字典与 Schema 层 (types/index.ts, 官方渠道字典)  │
├─────────────────────────────────────────────────────────────┤
│ Layer 0: 母宪法公理层 (净资产恒等式、物理留痕、零副作用、单点权威)  │
└─────────────────────────────────────────────────────────────┘
```

---

## 🛡️ 二、开发四大核心铁律公理

1. **【单一权威业务入口公理 (Single Point of Authority)】**：
   - 调额对账必须 100% 收编至 `AccountBalanceAdjustModal`；
   - 记账操作必须统一通过 `UniversalQuickAddModal` 或 `api.createTransaction`；
   - 严禁在“设置页”等非对账位置开设绕过复式记账平衡的“改余额后门”。
2. **【官方渠道 100% 真实还原公理】**：
   - 微信、支付宝、各大银行、京东白条、美团月付等**官方渠道必须 100% 还原官方品牌色、官方图标与规范名称**；
   - 用户自定义资产与心愿目标才采用自研定制样式。
3. **【轻量化现代算力 Web 动效公理】**：
   - 采用纯 CSS GPU 硬件加速的液态毛玻璃（`backdrop-filter`）、SVG 图标与 `canvas-confetti` 粒子礼花；
   - **严禁引入任何几十 MB 的重型 MP4 视频或高清 GIF**，确保包体极速秒开、全端 60FPS 满帧运行。
5. **【UI 审美标准法与纵向空间紧凑公理】**：
   - 遵循 `docs/MASTER_CONSTITUTION.md` 第五章，卡片 padding 控制在 `p-3.5 sm:p-4`，容器间距 `space-y-2.5 sm:space-y-3`；
   - 严禁过高臃肿卡片，确保首屏信息紧凑、排版舒适、呼吸感与高密度并存。
4. **【零副作用与纯函数红线】**：
   - 严禁在 Getter / Computed / 渲染函数中产生修改状态的副作用；
   - 交付前自动化测试与真实 API 拟人实测必须 **100% PASS**。

---

## ⚡ 三、动态修宪与违宪冲突仲裁机制

当用户提出任何新点子、新需求或修改指令时，你必须强制执行以下 4 步闭环：
1. **自动层级定级 (Auto-Triage)**：研判需求属于 Layer 0~4 哪一层；
2. **违宪冲突探测 (Conflict Detection)**：若需求与既有核心公理（如复式记账恒等、防重复扣款）产生冲突，**严禁私自盲改！** 必须即刻向用户主动预警并提供结构化选项（A. 正式修宪 / B. 遵宪替代方案）；
3. **即时同步入宪与活文档 (Living Docs)**：修改代码的同时，强制同步更新 `docs/` 文档、`CHANGELOG.md` 与测试套件；
4. **Git 物理留痕抗遗忘**：每次改动必须包含精准的 Git 提交记录。

---

## 🎯 四、手术级 3 步作业法 (Surgical Dispatch)

1. **查表定位**：在动工前先查阅 `docs/SURGICAL_DISPATCH_GUIDE.md` 确定 `[OP-XXX]` 调度编号与靶向文件，严禁漫无目的全仓盲搜；
2. **纯净作业**：
   - 算法/规划变动 $\to$ 改 `services/` 纯计算引擎；
   - 交互/视效变动 $\to$ 改 `components/` 或 `pages/`；
   - 存储/去重变动 $\to$ 改 `localStore.ts` 或 `api/client.ts`；
3. **秒级自愈与拟人实测**：运行 `npm test`（含 10 轮自检 + 真实 API 拟人化实测），全绿方可交付！
